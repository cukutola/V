<Window x:Class="Vereinssoftware.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vereinssoftware - Login" 
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Vereinssoftware" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Benutzername:" Margin="0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5" Padding="5"/>

        <TextBlock Grid.Row="3" Text="Passwort:" Margin="0,5"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" 
                     Margin="0,5" Padding="5"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="5" Margin="0,20,0,0">
            <Button Content="Anmelden" Command="{Binding LoginCommand}" 
                    Padding="10,5" Margin="0,5" IsDefault="True"/>
            
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       HorizontalAlignment="Center"
                       Margin="0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            
            <TextBlock Text="Standard-Login: admin / admin" 
                       FontSize="10" 
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       Margin="0,10"/>
        </StackPanel>
    </Grid>
</Window>
