<UserControl x:Class="Vereinssoftware.App.Views.MemberManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mitgliederverwaltung" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="5" Margin="0,0,10,0"
                     VerticalAlignment="Center"/>

            <Button Grid.Column="1" Content="Hinzufügen" 
                    Command="{Binding AddMemberCommand}"
                    Padding="10,5" Margin="5,0"/>
            <Button Grid.Column="2" Content="Bearbeiten" 
                    Command="{Binding EditMemberCommand}"
                    Padding="10,5" Margin="5,0"/>
            <Button Grid.Column="3" Content="Löschen" 
                    Command="{Binding DeleteMemberCommand}"
                    Padding="10,5" Margin="5,0"/>
        </Grid>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Members}"
                  SelectedItem="{Binding SelectedMember}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Nachname" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="E-Mail" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Mitglied seit" Binding="{Binding MemberSince, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
