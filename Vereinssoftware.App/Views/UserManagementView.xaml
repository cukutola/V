<UserControl x:Class="Vereinssoftware.App.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Benutzerverwaltung" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Benutzer hinzufügen" 
                    Command="{Binding AddUserCommand}"
                    Padding="10,5" Margin="0,0,5,0"/>
            <Button Content="Löschen" 
                    Command="{Binding DeleteUserCommand}"
                    Padding="10,5" Margin="5,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Benutzername" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Nachname" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="E-Mail" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Rolle" Binding="{Binding Role}" Width="100"/>
                <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
